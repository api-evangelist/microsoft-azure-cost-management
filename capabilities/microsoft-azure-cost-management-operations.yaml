naftiko: 1.0.0-alpha2
info:
  label: Azure Cost Management REST API — Operations
  description: 'Azure Cost Management REST API — Operations. 1 operations. Lead operation: Azure Cost Management REST API
    List operations. Self-contained Naftiko capability covering one Microsoft Azure Cost Management business surface.'
  tags:
  - Microsoft Azure Cost Management
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_COST_MANAGEMENT_API_KEY: MICROSOFT_AZURE_COST_MANAGEMENT_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoft-azure-cost-management-operations
    baseUri: https://management.azure.com
    description: Azure Cost Management REST API — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.CostManagement-operations
      path: /providers/Microsoft.CostManagement/operations
      operations:
      - name: listoperations
        method: GET
        description: Azure Cost Management REST API List operations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_COST_MANAGEMENT_API_KEY}}'
  exposes:
  - type: rest
    namespace: microsoft-azure-cost-management-operations-rest
    port: 8080
    description: REST adapter for Azure Cost Management REST API — Operations. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-costmanagement/operations
      name: providers-microsoft-costmanagement-operations
      description: REST surface for providers-Microsoft.CostManagement-operations.
      operations:
      - method: GET
        name: listoperations
        description: Azure Cost Management REST API List operations
        call: microsoft-azure-cost-management-operations.listoperations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoft-azure-cost-management-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Azure Cost Management REST API — Operations. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: azure-cost-management-rest-api
      description: Azure Cost Management REST API List operations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoft-azure-cost-management-operations.listoperations
      outputParameters:
      - type: object
        mapping: $.
